'use client'
import type { CVSection } from '@/modules/cv/schema'
type Props = { sections: CVSection[]; onToggleVisibility: (id: string) => void }
export function SectionRail(_props: Props) { return null }
